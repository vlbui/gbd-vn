# 03_level2_ncd

Placeholder. See [plans/PUBLICATION_PLAN.xlsx](../../plans/PUBLICATION_PLAN.xlsx) for title, target journal, and timeline.

This project directory is scaffolded but not yet populated. When starting this paper:

1. Create subfolders as needed (`scripts/`, `notebooks/`, `data/`, `figures/`, `tables/`, `manuscript/`).
2. Import shared utilities: `from shared import <helper>`.
3. Read from `shared_processed/` (multi-paper) or `raw_data/` (IHME native).
4. Write paper-specific outputs into this project folder.
